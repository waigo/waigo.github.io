mixin siteDetails(idx, offset)
  - var project = projects[idx]
  if project
    div(class="item col-md-5 col-md-offset-" + offset)
      h2.title
        a(href=project.url) #{project.title}
      p.description #{project.body}
  

extend layout

block content

  section#projects
    .container
      .row
        .col-md-10.col-md-offset-1.col-xs-12(role="main")
          div.intro
            :markdown
              Projects built using Waigo. [Raise a pull request](https://github.com/waigo/waigojs.com-generator/blob/master/backend/projects.js) to add your own.

          ul
            each val, idx in projects
              if 0 === idx % 2
                li.row
                  +siteDetails(idx, 0)
                  +siteDetails(idx + 1, 2)

